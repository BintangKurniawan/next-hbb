import Template from "@/components/Template";
import React from "react";

const dashboard = () => {
  return (
    <Template>
      <p>You can type everything inside template</p>
    </Template>
  );
};

export default dashboard;
